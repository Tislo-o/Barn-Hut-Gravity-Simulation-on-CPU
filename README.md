Set GENERATE_PNGS and RENDER_ON_WINDOW either to true or false depending on your wish.
1)If GENERATE_PNGS = true, the program will generate pngs images of each frame in a frame folder at in the same folder as your .exe (make sure to create it first otherwise it will complains)
